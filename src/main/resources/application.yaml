spring:
  application:
    name: sequence-service
  datasource:
    url: jdbc:h2:mem:sequenceDB
    driver-class-name: org.h2.Driver
    username: root
    password: root
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
